<div class="row">
	<div class="col-md-6 col-md-offset-3 col-xs-10 col-xs-offset-1">
	   <h1><span>
       <%= form_tag("/locations/search", method: "get") do %>
          <!-- <%= label_tag(:q, "Search for:") %> -->
          <%= text_field_tag 'search', nil, placeholder: 'city state' %>
          <%= submit_tag("Search") %>
        <% end %>
     </span></h1>
  </div>
</div>
